// Adds extra Jest matchers that understand the DOM — e.g. toBeInTheDocument(),
// toHaveTextContent(), toBeDisabled(). Without this import those matchers don't exist.
import "@testing-library/jest-dom";
